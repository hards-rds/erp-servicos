import { notFound } from "next/navigation"; import { PageHeader } from "@/components/layout/page-header"; import { DriverForm, type DriverFormValue } from "@/components/transport/driver-form"; import { getTransportContext } from "@/lib/transport/server";
export default async function EditDriverPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const context = await getTransportContext("motoristas", "editar"); if (!context.ok) notFound(); const { data } = await context.supabase.from("transport_drivers").select("*").eq("company_id", context.profile.company_id).eq("id", id).maybeSingle(); if (!data) notFound(); return <><PageHeader area="Transporte / Motoristas / Editar" title={data.full_name} description="Atualize CNH, contato e disponibilidade." action={<a className="ghost-button button-link" href="/transporte/motoristas">Voltar para motoristas</a>} /><section className="form-panel page-form-panel"><DriverForm driver={data as DriverFormValue} /></section></>; }
