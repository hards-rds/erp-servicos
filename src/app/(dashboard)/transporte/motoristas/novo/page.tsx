import { PageHeader } from "@/components/layout/page-header"; import { DriverForm } from "@/components/transport/driver-form"; import { getTransportContext } from "@/lib/transport/server";
export default async function NewDriverPage() { const context = await getTransportContext("motoristas", "criar"); return <><PageHeader area="Transporte / Motoristas / Novo" title="Novo motorista" description="Cadastre documentos, contato e tipo de vinculo." action={<a className="ghost-button button-link" href="/transporte/motoristas">Voltar para motoristas</a>} />{context.ok ? <section className="form-panel page-form-panel"><DriverForm /></section> : <div className="form-error">Este cadastro nao esta disponivel para a empresa ativa.</div>}</>; }
